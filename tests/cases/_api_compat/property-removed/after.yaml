# expect: breaking property-removed
#
# `size` is gone. Every XR that set it now carries a field the schema does not
# describe, and Kubernetes prunes it on the next write — silently.
apiVersion: apiextensions.crossplane.io/v2
kind: CompositeResourceDefinition
metadata:
  name: xwidgets.platform.wxops.cloud
spec:
  group: platform.wxops.cloud
  scope: Namespaced
  names:
    kind: XWidget
    plural: xwidgets
  versions:
    - name: v1alpha1
      served: true
      referenceable: true
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              properties:
                parameters:
                  type: object
                  required:
                    - name
                  properties:
                    name:
                      type: string
            status:
              type: object
              properties:
                ready:
                  type: boolean
