[package]
name = "tenant-database"
version = "0.1.0"
edition = "v0.12.1"
description = "KCL composition logic for XTenantDatabase — CNPG Database CR + provider-sql Role + ESO/Vault sync and rotation."

[dependencies]
