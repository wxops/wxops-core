[package]
name = "tenant-app"
version = "0.1.1"
edition = "v0.12.1"
description = "KCL composition logic for XTenantApp — Deployment/Service/Ingress app scaffold with optional Vault ESO env secrets and a composed tenant database."

[dependencies]
