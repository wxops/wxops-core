# Cluster-scoped composite resource (no namespace).
# Use claim.yaml instead if you want namespace-scoped access.
apiVersion: platform.wxops.cloud/v1alpha1
kind: XRandomPassword
metadata:
  name: example-password
spec:
  parameters:
    length: 32
    special: true
    overrideSpecial: "!@#$%^&*"
